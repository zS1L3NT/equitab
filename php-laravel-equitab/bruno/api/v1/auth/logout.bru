meta {
  name: logout
  type: http
  seq: 2
}

post {
  url: {{HOST}}/api/v1/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
      "username": "zS1L3NT",
      "password": "P@ssw0rd",
      "device_name": "test"
  }
}

script:post-response {
  bru.setEnvVar("TOKEN", res.body?.data?.token || "")
}
